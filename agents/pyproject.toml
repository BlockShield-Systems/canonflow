[project]
name = "canonflow-agents"
version = "0.1.0"
description = "CanonFlow agents for the Google Cloud Agentic Cinema Hackathon"
requires-python = ">=3.12,<3.13"
dependencies = [
    "google-adk[mcp]==2.7.1",
    "sqlglot>=30.17.0",
]

[tool.uv]
package = false
